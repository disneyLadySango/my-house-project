import React from 'react';
import Icon, { IconEnum } from '../../atoms/Icon'

const RemoconPage: React.FC<any> = props => (
  <div>
    <h1>Hello</h1>
    <Icon size={23} image={IconEnum.AIRCON} />
  </div>
)

export default RemoconPage
